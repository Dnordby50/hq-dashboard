## [2026-07-21 MST] Cowork: scoped Routemize -> TopCoat appointment intake (prompt 43 written)

By: Cowork

Changed: No repo code beyond docs. Wrote claude-code-prompt-43-routemize-appt-intake.md to the repo root and delivered it to Dylan. Scoped the Routemize -> TopCoat appointment integration after grounding in the live code. (Numbering note: 40 = invoicing/manual-completion, 41 = reserved editable combined-send, 42 = drip go-live, so this Routemize prompt is 43. An earlier draft this session was mis-named prompt 40; the mis-named file was removed.)

CONTEXT SHIFT (Dylan, this session): DripJobs is being turned off today/tomorrow. Routemize (prescottepoxycompany.routemize.com) becomes the booking front end; TopCoat's pec_appointments (0 rows in prod) becomes the appointment system of record. So this integration is now the ONLY path appointments enter TopCoat, not a side channel.

ARCHITECTURE DECISION (mentor push-back, Dylan agreed): Dylan initially wanted the Google-Calendar-into-TopCoat route ("how DripJobs works"). Verified that route is already built (pec-google-calendar-pull.cjs already ingests external Google events into pec_appointments every 15 min), but it is LOSSY: events arrive with no lead/customer link, appt_type forced to 'other', and rep = whichever calendar they land on. That breaks the lead -> drip -> appointment thread Dylan wants to smoke test. Recommended and Dylan approved the DIRECT path instead: Routemize -> Zapier -> a new pec-appt-intake endpoint, which preserves rep + type and matches the appointment to the lead by phone. Google-calendar visibility still comes free from the EXISTING pec-appt-sync-push.cjs, so Dylan keeps the Google view he likes without the data loss.

LOCKED DECISIONS baked into prompt 43: (1) direct intake endpoint, full mirror (created/updated/cancelled/deleted). (2) Routemize delete -> mark canceled, not hard-delete. (3) match lead then customer by phone/email and link; no auto-create of leads (avoids colliding with Meta/DripJobs intake). (4) Google visibility via the existing push only, best-effort. (5) rep mapping to pec_sales_team_members by google_email else name; unmatched -> unassigned. (6) default appt_type on_site_estimate.

KEY FINDINGS in the prompt: pec_appointments.source is CHECK-constrained to topcoat/google only and has no external-id column, so a migration is required (add routemize_appt_id + partial unique index; extend the source CHECK to allow 'routemize' - the known material_type CHECK gotcha). Intake reuses PEC_WEBHOOK_SECRET (already in Netlify) and the pec-lead-intake shape (badSecret, logIngest, last-10 phone dedupe). The reminder engine runApptReminders already auto-confirms linked+consented appointments via the 15-min scheduled runner. The connected-pipeline requirement: the intake must reproduce the in-app booking side effects (apptBookingSideEffects / log_appointment_booked) so a linked lead advances stage AND its nurture drip pauses; prompt 43 tells Claude Code to factor that into a server helper and to ADD the drip-pause if the in-app flow doesn't already do it (flagged as a small behavior addition).

Why: Dylan's request to connect Routemize to TopCoat via Zapier and push scheduled appointments onto the TopCoat appointment calendar, plus a full end-to-end smoke test (lead intake -> drip -> appointment) once live.

Files touched: claude-code-prompt-43-routemize-appt-intake.md (new, repo root), PROJECT-LOG.md (this entry).

Next steps: Dylan hands prompt 43 to Claude Code. Claude Code builds the endpoint + migration; Cowork applies the migration + regenerates SCHEMA.md; Dylan deploys. Then Cowork builds the Routemize Zaps (needs Dylan logged into Zapier + the live intake URL) and runs the end-to-end smoke test.

Handoff to Dylan: git add + commit this log entry and claude-code-prompt-43-routemize-appt-intake.md (the Cowork cloud sandbox cannot git commit), then hand prompt 43 to Claude Code. Log into Zapier when ready so Cowork can wire the Zaps after the endpoint deploys.

---

