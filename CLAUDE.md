# TopCoat: Claude Code entry point

@AGENTS.md

`AGENTS.md` is the canonical shared contract. It replaces the duplicated rules formerly kept here. Read `docs/product-charter.md` once per task, then use the scoped context command described in the contract. Detailed engineering guidance is loaded only for the affected module. Do not import the historical handover or full project log at startup.
